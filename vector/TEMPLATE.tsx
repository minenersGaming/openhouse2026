import React, { FC } from "react";

const Lamp: FC<{
  className?: string;
}> = ({ className }) => {
  return <></>;
};

export default Lamp;

/*

 style={{ maskType: 'alpha' }}

 <g style={{ mixBlendMode: 'soft-light' }}>

 className={`${className}`} 

 */
